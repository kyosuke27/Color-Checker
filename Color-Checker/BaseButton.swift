import SwiftUI

struct BaseButton: View {
    var body: some View {
        Button {
        } label: {
            Text("←")
        }
        .padding()
        .background(

    }
}

#Preview {
    BaseButton()
}
